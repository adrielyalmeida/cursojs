const nomes = ['ana júlia', 'Caio vinicius', 'BIA silva'];

const nomesPadronizados = nomes.map((nome) => nome.toUpperCase());  // toUpperCase transforma em maiúsculas

console.log(nomesPadronizados);